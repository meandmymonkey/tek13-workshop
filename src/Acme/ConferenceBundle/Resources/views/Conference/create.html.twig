{% extends 'AcmeConferenceBundle::layout.html.twig' %}

{% block content %}

    <h1>Create new Conference</h1>

    <form action="{{ path('conference_create') }}" method="post" novalidate="novalidate">
        {{ form_widget(form) }}
        <button type="submit">Save</button>
    </form>

{% endblock %}
